<div id="inputTypeContainer" class="soft-card">
    <div class="summary-label text-center mb-2">
        <i class="bi bi-info-circle me-1"></i>Giới hạn nhập liệu
    </div>
    <table class="table table-bordered table-sm text-center mb-0">
        <thead class="table-light">
            <tr>
                <th id="inputTypeHeader" style="font-size: 0.85rem;">Loại dữ liệu</th>
            </tr>
        </thead>
        <tbody id="inputTypeBody">
            <tr>
                <td class="text-muted small">
                    Click vào một <strong>ô trống</strong> để xem ký hiệu hỗ trợ.
                </td>
            </tr>
        </tbody>
    </table>
</div>

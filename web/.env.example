# Vite: biến có prefix VITE_ được INLINE vào JS bundle LÚC BUILD.
# → không có env runtime; đổi domain prod = sửa file này trên VPS + npm run build lại.
# → vì build trực tiếp trên VPS nên không cần CI build-args; không có Dockerfile.

VITE_API_BASE_URL=http://localhost:5080
VITE_APP_NAME=MTCA
